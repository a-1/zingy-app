'use strict';

define(function (require) {
    var angular = require('angular');

    var dependencies = [

        //load all components here
        //require('auth').name

    ];

    return angular.module('components', dependencies);

});