'use strict';

define(['angular'], function (angular) {

    return angular
        .module('suppliers.update.ctrl', [])
        .controller('suppliers.update.ctrl', function ($scope) {
            $scope.title = 'Zingy is now JustKhelo';
        });

});